import { PortableTextBlock } from '@portabletext/types';
import { ReactNode } from 'react';

type Props = {
  children?: ReactNode;
  value: PortableTextBlock;
};

export default function BlockGrid({ children, value }: Props) {
  // Pragraphs
  return <></>;
}
